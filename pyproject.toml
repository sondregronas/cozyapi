[project]
name = "cozy"
version = "0.1.0"
description = "A simple API wrapper to generate images using ComfyUI"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "aiohttp>=3.14.3",
    "fastapi>=0.141.1",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.52.0",
    "websockets>=16.1.1",
]
